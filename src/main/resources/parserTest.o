class test extends stuff is
    var counter : Integer
    method MaxInt(a: Array[Integer]) : Integer is
        var max : Integer
        var i : Integer

        max := Integer.Min()
        i := Integer(1)
        while i.Less(a.Length) loop
           if a.get(i).Greater(max) then max := get(i) end
           i := i.Plus(1)
        end

        var a : Array[Integer]

        return max
    end

    this(A: Integer) is
    counter := A
    end

end


class C[T] is
    var m : T
end